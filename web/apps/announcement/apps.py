from django.apps import AppConfig


class AnnouncementConfig(AppConfig):
    """Класс для конфигурации приложения `announcement`."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.announcement"
